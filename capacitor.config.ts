import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-app-from-udemy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
